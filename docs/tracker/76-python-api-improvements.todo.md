# Issue 76: Python API improvements

## Problem

The Python API could be more ergonomic and complete.

## Scope

- Export all parsers from top-level package
- Add convenience functions: `render_to_file()`, `render_to_png()`
- Better error messages for unsupported syntax
- Ensure consistent API across all diagram types
