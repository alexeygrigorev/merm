# Issue 77: Theme customization

## Problem

Theme customization is limited. Users can't easily change colors or styles.

## Scope

- Allow passing custom theme/colors via API and CLI
- Support mermaid theme directives (%%{init: {'theme': 'dark'}}%%)
- Built-in themes: default, dark, forest, neutral
- Expose theme configuration in both CLI and API
